"""Разовая выдача разборов тем, кто завёлся раньше раздела «Документы».

Разборы выдаются при регистрации, поэтому у всех, кто завёлся до выкладки
раздела, их оказалось ноль — и раздел встретил их пейволлом вместо работы.
Для человека это выглядит как поломка: три бесплатных обещаны, а их нет.

Начисляем только тем, у кого ровно ноль. Кто успел зарегистрироваться после
выкладки, свои три уже получил, и повторять выдачу ему незачем. Крайний
случай — человек, который все три уже потратил: он получит их ещё раз. Это
известная и осознанная щедрость: раздел прожил в бою считаные часы, таких
людей единицы, а отличить их от старых пользователей нечем — отметки о
выдаче у разборов нет.

Обратная миграция ничего не отматывает: отбирать подаренное на откате хуже,
чем оставить.

Revision ID: f8c3d0a91b62
Revises: d5a2c81e7f43
Create Date: 2026-09-19 16:40:00.000000+00:00
"""

from __future__ import annotations

from alembic import op

revision: str = "f8c3d0a91b62"
down_revision: str | None = "d5a2c81e7f43"
branch_labels: str | None = None
depends_on: str | None = None

#: Столько же, сколько выдаётся при регистрации (SIGNUP_DOCUMENTS по
#: умолчанию). Числом в самом запросе, а не настройкой: миграция — снимок
#: решения на своём моменте, и переезжать вслед за настройкой она не должна.


def upgrade() -> None:
    op.execute("UPDATE users SET bonus_documents = 3 WHERE bonus_documents = 0")


def downgrade() -> None:
    """Подаренное не отбираем — см. заголовок."""
